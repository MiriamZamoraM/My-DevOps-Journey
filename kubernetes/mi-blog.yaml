apiVersion: v1
kind: Service

metadata:
  name: servicio-blog
  labels:
    app: blog

spec:
  type: NodePort
  selector:
    app: blog
  
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30007